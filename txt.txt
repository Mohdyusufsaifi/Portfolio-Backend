
  // transporter.sendMail(mailOptions, function (error, info) {
  //     if (error) {
  //         console.log(error);
  //     } else {
  //       res.status(200).send(setTimeout(() => {`<div class="alert alert-primary" role="alert">SuccessFully</div>`}, 3000));
  //     }
  //   })
  // }
  // );
  // res.status(200).send(`<script>window.location.assign("http://localhost:3000/")</script>`)

